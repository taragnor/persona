{
  "name": "persona",
  "version": "1.0.0",
  "description": "Foundry VTT system for Persona custom system",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc && copyfiles -u 1 src/**/*.css src/**/*.json src/**/*.hbs dist/ && copyfiles README.md dist/"
  },
  "author": "taragnor",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taragnor/gm-paranoia-taragnor"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "copyfiles": "^2.4.1",
    "typescript": ">=5.3.3"
  }
}
