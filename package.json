{
  "name": "persona",
  "version": "1.0.0",
  "description": "Foundry VTT system for Persona custom system",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc && copyfiles -u 1 src/**/*.css src/**/*.json src/**/*.webp src/**/*.hbs src/**/*.mp3 src/**/*.wav src/**/*.png src/**/*.gif dist/ && copyfiles README.md dist/"
  },
  "author": "taragnor",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taragnor/gm-paranoia-taragnor"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "copyfiles": "^2.4.1",
    "typescript": ">=5.3.3",
    "@typescript-eslint/parser": "^8.41.0",
    "eslint": "^9.34.0",
    "typescript-eslint": "^8.41.0"
  }
}
