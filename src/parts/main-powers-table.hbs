<div class="main-powers">
	{{#each (iff activePowersOnly persona.activeCombatPowers persona.mainPowers) as |power i|}}
	<div class="main-power" data-power-id="{{power.id}}">
		<div class="power-name-block
		{{#if (and (not ../activePowersOnly) (lte ../persona.maxMainPowers i))}} over-max-power {{/if}}
		{{#if (isPowerIllegal ../persona power)}} illegal-level {{/if}}">
			<span class="die-with-text">
				{{#if (and ../usePowers (canUsePower ../persona power))}}
				<a class="rollPower" >
					<img class="small-die" src="icons/dice/d20black.svg">
				</a>
					{{/if}}
					<a class="powerName" title='{{> "systems/persona/parts/power-tooltip-lite.hbs" actor=../actor CONST=../CONST power=power}}'>
						{{> "systems/persona/parts/pretty-power-name.hbs" CONST=CONST power=power user=../persona}}
						<span class="cost-string">
							({{powerCostString power ../persona}})
						</span>
					</a>
			</span>
		</div>
		<div class="power-operations">
			<a class="sort-up">
				<i class="fa-solid fa-sort-up"></i>
			</a>
			<a class="sort-down">
				<i class="fa-solid fa-sort-down"></i>
			</a>
			{{#if (canUseSideboard ../persona.user)}}
			<a class="move-to-sideboard" title="Move to Sideboard">
				<i class="fa-solid fa-people-carry-box"></i>
			</a>
			{{/if}}
			{{#if (and (canDeletePowers ../persona) ../canDelete)}}
			<a class="delPower" title="Delete Power">
				<i class="fa-solid fa-trash-can"></i>
			</a>
			{{/if}}
		</div>
	</div>
	{{/each}}
</div>

